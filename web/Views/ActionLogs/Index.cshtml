@model IEnumerable<web.Models.ActionLog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                ActionLog Code
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActionLogID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ActionLogID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ActionLogID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
